{
	"name": "{{name}}",
	"version": "1.0.0",
	"description": "{{description}}",
	"main": "index.js",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1",
		"build": "webpack --mode production",
		"dev": "webpack --mode development --watch"
	},
	"author": "{{author}}",
	"license": "MIT",
	"devDependencies": {
		"scriptcat-webpack-plugin": "^1.1.0",
		"webpack": "^5.61.0",
		"webpack-cli": "^4.9.1"
	}
}